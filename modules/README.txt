This directory is for the contrib modules required by the orgRight Client system
plus the orgRight custom modules (in the orgright sub-directory).

Any modules required for a special client or environment should go into:
* sites/site.domain.name/modules, or
* sites/all/modules
respectively.

